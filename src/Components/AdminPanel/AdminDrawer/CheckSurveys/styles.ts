export const grid = {
    '& > :not(style)': {
        m: 1,
        width: 256,
        height: 256,
        padding: 5,
    },}

export const paper = {
    cursor: 'pointer',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
}