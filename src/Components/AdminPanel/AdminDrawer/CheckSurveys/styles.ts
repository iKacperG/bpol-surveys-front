export const grid = {
    '& > :not(style)': {
        m: 1,
        width: 128,
        height: 128,
    },}

export const paper = {
    cursor: 'pointer',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
}